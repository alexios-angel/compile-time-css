This repository ("compile-time-css" / ctcss) is a derivative work
built on:

    notre (a fork of Compile Time Regular Expressions)
    https://github.com/alexios-angel/notre

which is itself a derivative of:

    Compile Time Regular Expressions (CTRE)
    https://github.com/hanickadot/compile-time-regular-expressions
    Copyright (c) Hana Dusikova and the CTRE contributors

License: Apache License 2.0, with LLVM Exceptions (see LICENSE in this
repository, reproduced unmodified from upstream).

What is derived
---------------
The CTLL compile-time LL(1) parser (ctll::fixed_string and supporting
headers, provided by the compile-time-lark submodule) is copied from
notre, where it descends from Hana Dusikova's CTRE with modifications.
This repository began as a clone of its sibling compile-time-html
(https://github.com/alexios-angel/compile-time-html) - itself a clone
of compile-time-xml - and keeps that git history; the build and
packaging scaffolding follows the sibling projects compile-time-json
and compile-time-json5 (https://github.com/alexios-angel/). The CSS
grammar, the stylesheet types, the selector-matching and cascade
layer, the value parsers and the ctcss API are original to this
repository.

Attribution for the parser architecture and the CTLL engine belongs to
Hana Dusikova; this file preserves it as required by Section 4(c) of
the Apache License 2.0.

The grammar layer is ctlark
(https://github.com/alexios-angel/compile-time-lark), a git submodule
under external/: the CSS grammar is written in the Lark grammar
language, designed by Erez Shinan and the lark-parser project
(https://github.com/lark-parser/lark, MIT license); see ctlark's
NOTICE for the full lineage, including the Tablewright-generated
grammar-of-grammars table.

The CSS language semantics implemented here (selectors, specificity,
the cascade) follow the W3C CSS specifications.
